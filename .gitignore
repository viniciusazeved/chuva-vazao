.venv/
__pycache__/
*.pyc
.claude/
.pytest_cache/
.ruff_cache/

# uv.lock nao versionado — contem pywin32 Windows-only que quebra
# build no Streamlit Cloud (Linux). Reproducibilidade via pyproject.toml
# + requirements.txt.
uv.lock

# Dados brutos nao redistribuidos (MDB original, caches GEE)
data/mdb_origem.mdb
data/dems/
data/gee_cache/

# SQLite extraido EH redistribuido como referencia (HidroFlu v2.0 freeware)
# entao nao ignoramos data/chuvavazao.db.

# Saidas de scripts de demo
out/

# Secrets
.streamlit/secrets.toml
.env
